import { Outlet } from "react-router-dom";

const FitnessRooms = () => {
    return (
        <div id="salas-preparacion-content">
            <Outlet />
        </div>
    );
}

export default FitnessRooms;
