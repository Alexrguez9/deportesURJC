import { Outlet } from "react-router-dom";

const LigasInternas = () => {
    return (
        <div id="ligas-internas-content">
            <Outlet />
        </div>
    );
}

export default LigasInternas;
